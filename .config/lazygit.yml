os:
  edit: 'nvim "{{filename}}"'
  editAtLine: 'nvim "{{filename}}" +"{{line}}"'
  editAtLineAndWait: 'nvim "{{filename}}" +"{{line}}"'
  open: 'nvim "{{filename}}"'

promptToReturnFromSubprocess: false
