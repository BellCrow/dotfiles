git config --global pull.rebase false

git config --global user.email "joshua.krieger@gmx.de"
git config --global user.name "Joshua Krieger"

# git helpers
alias ec="git commit --allow-empty -m \"- Trigger CI\" && git push"
